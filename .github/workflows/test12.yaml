name: Concurrency Demo

on:
  push:
    branches: [ main ]

concurrency:
  group: main-branch-group
  cancel-in-progress: true

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running workflow"
      - run: sleep 30
      - run: echo "Finished again"