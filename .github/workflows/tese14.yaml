name: Job Timeout Example

on:
  workflow_dispatch:

jobs:
  sample:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Start
        run: echo "Job started"

      - name: Long sleep
        run: sleep 400

      - name: End
        run: echo "Job finished"